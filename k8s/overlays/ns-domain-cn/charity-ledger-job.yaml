apiVersion: batch/v1
kind: Job
metadata:
  name: charity-ledger-demo
  labels:
    edu.web3/plugin-id: edu.cn.trace.charity
    edu.web3/compliance: fabric-sandbox
    edu.web3/namespace: ns-domain-cn
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        edu.web3/plugin-id: edu.cn.trace.charity
    spec:
      restartPolicy: Never
      containers:
        - name: fabric-runner
          image: hyperledger/fabric-tools:2.5
          env:
            - name: CHAIN_ID
              value: "fabric-local"
            - name: FABRIC_CHANNEL
              value: "edu-cn-trace-sandbox"
            - name: FABRIC_ORG
              value: "OrgEduDemo"
            - name: CHAINCODE_PATH
              value: "plugins/charity-ledger/chaincode/charity_ledger.go"
            - name: TASK_TYPE
              value: "CN_CHARITY_LEDGER_DEMO"
          command:
            - sh
            - -c
            - |
              echo "[fabric-local] channel=${FABRIC_CHANNEL} org=${FABRIC_ORG}"
              echo "[charity-ledger] chaincode=${CHAINCODE_PATH} task=${TASK_TYPE}"
              echo "Charity donation ledger demo completed (simulated payments only)"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
